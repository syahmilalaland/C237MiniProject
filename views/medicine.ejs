<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Product Details</title>
</head>
<body>
    <div class="container my-5">
        <% if (medicines) { %> 
            <div class="card">
                <div class="card-header">
                    <h2><%= medicines.medicineName || 'Product Name' %></h2>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-4">
                            <img src="/images/<%= medicines.medicineImage || 'placeholder.jpg' %>" class="img-fluid" alt="<%= medicines.medicineName %>">
                        </div>
                        <div class="col-md-8">
                            <p><strong>Price:</strong> $<%= medicines.medicinePrice || 'N/A' %></p>
                            <p><strong>Description:</strong> <%= medicines.medicineDesc || 'No description available.' %></p> 
                        </div>
                    </div>
                </div>
            </div>
        <% } else { %>
            <div class="alert alert-warning" role="alert">
                No product found.
            </div>
        <% } %>
        <div class="mt-3">
            <a href="/" class="btn btn-primary">Back</a>
        </div>
    </div>
</body>
</html>
