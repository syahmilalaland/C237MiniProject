<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Product Details</title>
</head>

<style>
    .button {
            width: 100%;
            padding: 10px;
            background-color: #007BFF;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .button:hover {
            background-color: #0056b3;
        }
</style>
<body>
    <div class="container my-5">
        <% if (random) { %> 
            <div class="card">
                <div class="card-header">
                    <h2><%= random.randomName || 'Product Name' %></h2>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-4">
                            <img src="/images/<%= random.randomImage || 'placeholder.jpg' %>" class="img-fluid" alt="<%= random.randomName %>">
                        </div>
                        <div class="col-md-8">
                            <p><strong>Price:</strong> $<%= random.randomPrice || 'N/A' %></p>
                            <p><strong>Description:</strong> <%= random.randomDesc || 'No description available.' %></p>
                            
                            <a href="/addtocart" class="button" onclick="return confirm('Item added successfully')">Add to cart</a>
                        </div>
                    </div>
                </div>
            </div>
        <% } else { %>
            <div class="alert alert-warning" role="alert">
                No product found.
            </div>
        <% } %>
        <div class="mt-3">
            <a href="/" class="btn btn-primary">Back</a>
        </div>
    </div>
</body>
</html>
